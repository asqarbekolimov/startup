import { withLayout } from "@/layouts/layout";
import { CoursesPageComponents } from "@/page-component";
import React from "react";

const Courses = () => {
  return <CoursesPageComponents />;
};

export default withLayout(Courses);
